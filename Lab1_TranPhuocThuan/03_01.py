def tin_tong_so_chan(lst);
    tong = 0
    for num lst:
        if num % 2 == 0:
        tong += num
    return tong

input_list = input ("Nhap danh sach cac so, cach nhau bang dau phay:")
number = list(map(int, imput_list.s(lit(',')))
              
              tong_chan = tin_tong_so_chan(numbers)
              print("Tong cac so chan trong List la:", tin_chan)